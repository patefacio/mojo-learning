{
  "name": "Mojo Learning Environment",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "postCreateCommand": "bash -c 'curl -ssL https://magic.modular.com | bash && export PATH=\"$HOME/.modular/bin:$PATH\" && pip3 install jupyter notebook ipython && magic install mojo || echo \"Mojo installation requires authentication - run magic install mojo manually\"'",
  "forwardPorts": [8888],
  "customizations": {
    "vscode": {
      "extensions": ["ms-python.python", "ms-toolsai.jupyter"]
    }
  },
  "containerEnv": {
    "MAGIC_DIR": "/root/.modular",
    "PATH": "/root/.modular/bin:${PATH}"
  },
  "remoteUser": "root"
}
