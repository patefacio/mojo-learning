{
  "name": "Mojo Learning Environment",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "postCreateCommand": "pip install jupyter notebook ipython && magic install mojo || echo 'Magic installation will be handled separately'",
  "forwardPorts": [8888],
  "customizations": {
    "vscode": {
      "extensions": ["ms-python.python", "ms-toolsai.jupyter"]
    }
  },
  "containerEnv": {
    "MAGIC_DIR": "/home/codespace/.modular",
    "PATH": "/home/codespace/.modular/bin:${PATH}"
  },
  "remoteUser": "codespace"
}
