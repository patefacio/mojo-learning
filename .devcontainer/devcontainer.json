{
  "name": "Mojo Learning Environment",
  "image": "mcr.microsoft.com/devcontainers/base:debian",
  "postCreateCommand": "curl -ssL https://magic.modular.com | bash && export PATH=\"$HOME/.modular/bin:$PATH\" && pip3 install jupyter notebook ipython && magic install mojo || echo 'Mojo installation requires authentication - run: magic install mojo'",
  "forwardPorts": [8888],
  "customizations": {
    "vscode": {
      "extensions": ["ms-python.python", "ms-toolsai.jupyter"]
    }
  },
  "containerEnv": {
    "MAGIC_DIR": "/home/vscode/.modular",
    "PATH": "/home/vscode/.modular/bin:${PATH}"
  },
  "remoteUser": "vscode"
}
