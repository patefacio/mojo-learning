{
  "name": "Mojo Learning Environment",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "postCreateCommand": "pip3 install jupyter notebook ipython && magic install mojo || echo 'Magic installation will be handled separately'",
  "forwardPorts": [8888],
  "customizations": {
    "vscode": {
      "extensions": ["ms-python.python", "ms-toolsai.jupyter"]
    }
  },
  "containerEnv": {
    "MAGIC_DIR": "/root/.modular",
    "PATH": "/root/.modular/bin:${PATH}"
  },
  "remoteUser": "root"
}
